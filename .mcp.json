{
  "_comment": [
    "No MCP servers are declared here on purpose.",
    "",
    "Konnect (the KiCad MCP) is registered at USER scope instead, in",
    "~/.claude.json and ~/.codex/config.toml, so both agents find it in every",
    "project. Declaring it here as well was a name collision and broke the",
    "connection on 2026-09-14 - CONNECTION_CLOSED against a server that was",
    "provably healthy.",
    "",
    "If you are setting this project up on another machine, you need Konnect",
    "(C:/Users/Public/AI-Tools/konnect) registered for your agent, AND the environment variable",
    "",
    "    KICAD_API_SOCKET=ipc://<temp>/kicad/api.sock",
    "",
    "which is NOT optional on Windows: NNG makes an ipc:// endpoint a named",
    "pipe, not a file, so socket auto-detection fails and a client can decide",
    "KiCad is not running and edit the board file underneath it - the",
    "2026-09-07 board loss, recreated. Full detail in docs/TOOLS.md and",
    "docs/decisions/0013."
  ],
  "mcpServers": {}
}
