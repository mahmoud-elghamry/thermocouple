# Open issues

One line per issue, numbered and never renumbered. Close an issue by moving it
to the Closed section with the date and what actually fixed it — do not delete
it.

A review or an audit does **not** get its own file. Its findings become issues
here, or they will be read once and forgotten.

Severity: **blocker** stops fabrication or release · **high** is a real failure
mode · **medium** costs time or quality · **low** is tidiness.

---

## Hardware

| # | Sev | Issue |
|---|---|---|
| I-058 | blocker | **K1 was wired with the coil on the wrong pins - FIXED IN THE SCHEMATIC 2026-09-24, the PCB and a bench check are still open.** `Relay:G5LE-1` draws the coil between **pins 2 and 5**, COM on 1, NC on 4 (the blade rests on it) and NO on 3; the footprint `Relay_THT:Relay_SPDT_Omron-G5LE-1` is numbered the same way, and the SPST-NO G5LE-1A has terminals 1, 2, 3 and 5 only, which is what leaves 4 as the NC contact. The generator had `1: RELAY_LOW, 4: RELAY_COM, 5: RELAY_NC`, so the MOSFET drain went to the contact COM and the coil sat between +24 V and the NC terminal of `J3`: **the relay could never be energised, the unit could never permit running, and a customer wire on J3-NC would have been tied to the coil.** Found while drawing the relay block, not by any gate - ERC cannot know which pins a relay's coil is on. Fixed: `1: RELAY_COM, 4: RELAY_NC, 5: RELAY_LOW` in `populate_schematic.py` and the schematic, and `netlist-baseline-reva1.json` changed by exactly those three pins (a deliberate circuit fix, not a baseline edited to pass). D3 now sits across the coil (K on +24 V, A on pin 5), `R53`/`R54` and `D4` still hang on `RELAY_LOW`, `J3` COM/NO/NC unchanged. **Omron's datasheet could not be fetched from this session** (egress blocked), so before power-up: ohmmeter across K1 pins 2-5 must read the DC24 coil (~2.9 kOhm), pins 1-4 must read 0 Ohm de-energised and 1-3 open. The PCB carries the corrected mapping since the 2026-09-26 regeneration (`I-061`); only the bench check remains. |
| I-055 | low | **Superseded by the redraw for the schematic's own coordinates, still true for new parts.** Downgraded from medium 2026-09-24. The sheet is no longer laid out from `populate_schematic.py`'s coordinates at all: `hardware/8ch/sch_layout/` places every symbol (channel template `channel.py`, blocks `extra.py`) on top of the generator's label-only output. The +284 mm offset now only matters for a part the generator adds before the layout pass is re-run on it. Original text in git history (commit 05d6abd). |
| I-059 | low | **Residual text overlaps on the redrawn sheet.** `reset_schematic_field_positions` puts Reference/Value where the library anchors them, and in the densest spots - the LM5164 feedback/ripple network, the RS-485 jumpers, `C4`/`C5` beside each MAX31856's value - some of that text still touches a neighbour. Readable, not tidy. Konnect has no tool to move a single field, so this is a KiCad-GUI touch-up (or a field-placement tool) rather than another layout pass. |
| I-063 | medium | **ERC differs between the cloud and the workstation.** `I-057` relinked `U12` to `Converter_DCDC_Isolated:IA0505S`; that library ships with KiCad 10.0.6 (cloud) but not 10.0.3 (this workstation, which has only `Converter_DCDC`). So `validate.ps1` here stops on 1 `lib_symbol_issues` warning (2026-09-26), while the cloud reports 0/0. Netlist and board unaffected. Fix: update the workstation to KiCad >= 10.0.6 (preferred - one KiCad version on both), or carry the symbol in a project library. Do not add the class to the tolerated list. |
| I-062 | low | **Split the schematic into hierarchical sheets on A3 - owner request 2026-09-25, AFTER `I-061` is closed.** Today it is one flat **A2** sheet (594 x 420 mm) with all 201 parts: correct, but hard to print or read (A3 prints at 70 %). Target (owner, 2026-09-25): **A4 per sheet**, A3 only for a sheet that turns out crowded (MCU or the top sheet) - a channel sheet **drawn once and instantiated 8 times** (KiCad reuses one sheet file; each instance gets its own designators), supply (24 V input, LM5164, protection), MCU (crystal, reset, LCD, buttons, ISP), relay + RS-485, and a top sheet wiring them. Reusing one channel file makes renamed channel nets **unavoidable** (`/TC1_FILT_P` -> `/TC1/FILT_P`), so for this route trap (1) below is not optional: the name-independent fingerprint and updated `apply_rules.py` patterns come first. **Connectivity must not change, so the PCB is not re-routed and Gerbers are unaffected.** Three traps, checked 2026-09-25: (1) `netlist_fingerprint.py` keys nets **by name**, and a hierarchical sheet prefixes local labels (`/TC1_FILT_P` -> `/Channel1/...`), so a correct split would still report every net removed/added; (2) `apply_rules.py` net-class patterns and `check_board.py` (`+3V3_SENS`, island membership) also match by name; (3) KiCad links footprints to symbols by hierarchical path, which changes. So: keep every existing net name (global labels / power symbols where needed, or a sheet-per-channel naming that reproduces `TCn_*` exactly), keep all reference designators, prove it with `netlist_fingerprint.py` = IDENTICAL against `netlist-baseline-reva1.json` and ERC 0/0, then *Update PCB from Schematic* with re-link by reference and a DRC showing zero new errors. If a name cannot be kept, add a name-independent comparison mode to the fingerprint first - never edit the baseline. Warning: an earlier hierarchical redraw was reverted because symbols lacked instance data and the netlist exported **zero components**; check the component count first. `sch_layout/build.py` works on a flat sheet and will need adapting or retiring. |
| I-026 | low | Ungrounded (insulated) junction probes would remove the cranking-current path between cylinders for free. Downgraded from blocker on 2026-09-07: the eight sensors sit on one engine block, so channel-to-channel differences are small, and the board is in its own panel 50 m away. Worth specifying if the supplier stocks them. **COSTED AGAINST LCSC ON 2026-09-16, and the answer inverts the premise of this row: the fix is CHEAPER than what is fitted.** The owner cannot get confirmation on the alternator and will not pay much, so the target became "cover the 58 V suppressed case with real margin, as cheaply as possible". **`LM5164DDAR` (`C477928`, $1.36, stock 6260): 6-100 V in, 1 A, synchronous buck.** Against the fitted `TSR 1-2450`, which LCSC prices at **$6.78** (`C6886877`): input ceiling 36 V -> **100 V** (42 V of margin over the 58 V load dump); cranking floor 6.5 V -> **6.0 V**, i.e. better, where the old R-78HB plan made it worse at 9 V; current 1 A kept, not halved; and **about $5 per unit cheaper** even after ~$0.40 of externals (47 uH `C167888` $0.05, 100 V 4.7 uF 1210 `C33546021` $0.13, bootstrap/VCC/RON/divider ~$0.03). **Three more input parts die at 58 V and were not previously flagged:** `D1` `SS34` is a **40 V** Schottky -> `SS310` 100 V (`C15874`, $0.033); `D2` `SMBJ33A` -> `SMBJ64A` (`C19077594`, $0.053 - 64 V standoff so it does not conduct during the load dump, 103 V clamp); `C53`/`C54` are 50 V parts -> 100 V, and the 47 uF `CP_Radial_D6.3mm` footprint will not hold a 100 V electrolytic (it can simply shrink - this row already proved the 47 uF buys only 5.7 ms and is not ride-through). **`F1` is a separate safety finding that comes straight from the source being a battery:** a 1206/1812 PTC has an interrupt rating around 40 A, and a 24 V battery will push thousands of amps into a short. A PTC is not a suitable sole protective device on a battery feed; an external inline fuse with real breaking capacity belongs in the panel, which is normal practice for battery-fed equipment anyway. **Honest limit of the proposal:** 100 V covers the *suppressed* load dump (ISO 16750-2 Test B, 58 V) with margin. A genuinely **unsuppressed** alternator reaches far higher and nothing at this price survives it - but internal suppression has been standard on alternators for decades and 58 V is what the vehicle-electronics industry designs to, so this is standard practice rather than a gamble. It is stated here instead of being hidden behind "100 V covers everything". **The cost that is real is not money:** a shielded module becomes a switching regulator on a board that measures 41 uV per degC, so this buys an EMC problem - run the `emc` skill on the result and keep the switcher out of the sensor island. Values not yet computed; the TI datasheet is needed for R_ON, the feedback divider and the ripple-injection network before anything is drawn. `docs/decisions/0009`. |
| I-027 | medium | Only applies if `I-026` goes ahead. With an ungrounded probe the input pair floats and `C2`/`C3` couple to `GND_SENS` for AC only. Confirm from the MAX31856 datasheet whether internal biasing defines the common mode; if not, one ~1 MOhm resistor per channel from `TC_FILT_N` to `GND_SENS`, which changes the schematic. |
| I-028 | medium | **RESOLVED IN THE SCHEMATIC 2026-09-16; downgraded from high.** The owner answered both halves: the supply is the **engine battery**, and confirmation on the alternator is not obtainable, with a cost ceiling on the fix. `docs/decisions/0016` records the outcome - **`LM5164DDAR` (6-100 V, 1 A, `C477928`, $1.36) replaces the `TSR 1-2450` ($6.78), so the fix is about $5 per unit CHEAPER than what it replaces**, keeps 1 A instead of halving to 0.5 A, and *improves* the cranking floor from 6.5 V to 6.0 V where the `R-78HB` plan would have made it worse at 9 V. `D1` 40 V -> `SS310` 100 V, `D2` `SMBJ33A` -> `SMBJ60A`, `C53`/`C54` 50 V -> 100 V. Verified: **ERC 0/0**, netlist 188/155/613 -> **201/162/645 with all 28 differences inspected**, the six new nets checked pin by pin against TI SNVSAU4D figure 7-1, BOM 86 lines / 201 parts with only the nine test points lacking an MPN, and `check_mpn_consistency.py` **128 checked, all match**. Values in `docs/reference/CALCULATIONS.md` section 1. **Still open, and why this is not closed:** (a) `F1` - a 1206 PTC interrupts about 40 A and a 24 V battery pushes thousands of amps into a short, so **a PTC is not an adequate sole protective device on a battery feed**; an external inline fuse with real breaking capacity belongs in the panel. (b) `D2` clamps at 96.8 V against the LM5164's **100 V absolute maximum - 3.2 V of margin** - and the ISO 7637-2 fast transients, which exist on a battery and not on a panel supply, have not been worked through at all. (c) A shielded module has become a 303 kHz switcher on a board that measures 41 uV per degC; the `emc` skill has not been run. (d) The 100 mA `ADM2587E` figure underpinning the load estimate is still an estimate. **Historical analysis below, kept because it is what ruled out the alternatives.**  **OWNER ANSWERED Q1 ON 2026-09-15: the 24 V comes from the engine battery, not a panel supply.** That settles the half that was undecided and confirms the input stage as fitted is inadequate - `TSR 1-2450` tops out at 36 V and the mildest load dump a 24 V system is specified for is 58 V. Q2 is still open (is the alternator suppressed), and a surge-stopper front end may make it moot - see the note at the end of this row. **The 24 V source changes the input stage - not just two parts.** Numbers below verified against datasheets and ISO 16750-2 on 2026-09-08. *From a panel supply off the mains:* the present PTC 0.5 A + SS34 + SMBJ33A + TSR 1-2450 is adequate, no change. *From the engine battery and alternator:* it is not, and the fix is larger than this issue used to claim. **Verified:** TSR 1-2450 input 6.5-36 V. SMBJ33A 33 V standoff, breakdown 36.7-40.6 V, clamping 53.3 V max, 600 W **at a 10/1000 us pulse**. R-78HB5.0-0.5L 9-72 V, 5 V, **0.5 A**, 3-SIP 17.5x11.5x8.5 mm. ISO 16750-2 load dump on a 24 V system: **58 V** with centralized suppression (Test B); Test A, unsuppressed, is far higher. **A TVS cannot protect against load dump, so "a clamp under the regulator's ceiling" is not implementable as written.** A load dump lasts ~350 ms - roughly 350,000x the 10/1000 us pulse the SMBJ33A is rated for. At 58 V the SMBJ33A is well past its 36.7 V breakdown, so it would conduct the entire pulse and fail. A TVS that survives needs standoff above 58 V (SMBJ58A), which clamps near 103 V - above the R-78HB's 72 V limit. **No SMBJ both survives a suppressed load dump and protects a 72 V regulator.** The real options are a regulator with ~100 V headroom, or an active load-dump clamp / series-FET protection IC (e.g. LM74930-Q1). **Cost if it goes ahead:** TSR 1-2450 $5.94 at qty 1 versus R-78HB5.0-0.5L $18.32 - **+$12.4 per unit** for the module alone, plus uprating `C53`/`C54` from 50 V (a 100 V 47 uF radial may not fit the 6.3 mm footprint), plus any active clamp. Call it **+$15-20 per unit**. **Current capability halves, 1 A to 0.5 A.** Estimated 5 V load: IA0505S ~265 mA + ADM2587E ~100 mA + MCU/LCD ~40 mA = ~405 mA, or 81 % of the R-78HB. The ADM2587E figure is an estimate and must be confirmed from its datasheet before committing to a 0.5 A part. **Suppression does NOT make a battery equivalent to a panel supply.** Even at 58 V the present TSR 1-2450 (36 V) still dies, so the regulator change is still required. **Cranking is a margin question, not a demonstrated failure** - an earlier draft of this row overstated it as "the engine cannot start", which the numbers do not support. Worked through on 2026-09-10: (a) `K1` is a G5LE-1 DC24 with must-operate 18 V but **must-release 2.4 V**, so a relay that is already energised holds through a dip to well under 18 V - the contact does not drop out. (b) `U14` regulates from **6.5 V** input, so the 5 V rail - and with it the 4.0 V BOD in `firmware/fuses.md` - survives any cranking dip that stays above 6.5 V, which a 24 V system normally does. (c) `C53` contributes nothing here: 47 uF from 24 V down to 6.5 V is 12.5 mJ, and at ~2.2 W input that is **5.7 ms** of ride-through against a dip lasting hundreds of ms. Riding through 200 ms would need ~1650 uF at 50 V, which does not fit the 6.3 mm radial footprint - **so cranking cannot be solved with a bigger capacitor on this board.** **What carries cranking is the regulator's minimum input voltage, and here the proposed replacement is worse than what is fitted:** TSR 1-2450 works from 6.5 V, R-78HB only from 9 V. Any regulator chosen for the battery case has to satisfy both ends - roughly 8 V to 100 V - not 9-72 V and not 6.5-36 V. Exact ISO 16750-2 cranking levels for 24 V systems were not obtainable from free sources and still need the standard itself. Fast ISO 7637-2 transients from other inductive loads on the same harness are present on a battery and absent on a panel supply, so the TVS is genuinely needed there. **Board space is not a constraint:** measured on 2026-09-10, `U14` at (197, 122) has 53 mm of clear board to its right and 18 mm to the bottom edge, with its nearest neighbour `C55` at 6.0 mm, so a larger regulator and even an active-clamp block of ~15x15 mm fit without moving the input cluster (`J1`/`F1`/`D1`/`D2`/`C53`/`C54`/`U14`, all inside x 170-212, y 86-131). **(1) ANSWERED: engine battery.** (2) **Still open: is the alternator load-dump suppressed?** **A third architecture, not costed before, may answer (2) by making it irrelevant and is better than the R-78HB route on every axis that matters here:** an active surge stopper in front of the *existing* `TSR 1-2450` - e.g. `LTC4364` (at LCSC, `C580878`), which clamps its output through an external N-FET during an overvoltage instead of trying to absorb it. It keeps the 6.5 V minimum input, so cranking stays as good as it is now rather than getting worse at 9 V; it keeps 1 A instead of halving to 0.5 A; and the ceiling is set by the FET's rating, so a 100 V FET covers the unsuppressed case too. **Not yet verified and the decision must not be taken on this row alone:** the FET has to survive ~350 ms in its linear region, which is a **safe-operating-area** check against a real SOA curve, not a voltage rating - that check is the whole risk of this approach. `LTC4364` price at qty 1 and the FET choice are also unpriced. `docs/decisions/0009`. |
| I-025 | high | Four-layer fabrication is hard to source in Egypt. The board is 4-layer because In1 carries three ground planes and In2 three supply planes. A 2-layer version is feasible - the analogue-critical copper is only the ~18x24 mm front end of each channel, and a local unbroken ground pour under those eight blocks preserves most of it - but it needs a larger board, the supply rails as wide top tracks, and bottom-layer jumpers everywhere except under the front ends. Measured today: 766 segments / 2761 mm on F.Cu, 278 / 3593 mm on B.Cu, 125 vias. Decide between a 2-layer redesign and ordering 4-layer from JLCPCB/PCBWay. |
| I-003 | high | `IA0505S` is unregulated; at ~12 % load its output can approach 6 V. Mitigated by moving to a 16 V LDO (`0002`), but the module's real output has never been measured on hardware. |
| I-004 | high | The isolation barrier has never been verified electrically. `.kicad_dru` enforces 3 mm, chosen from the tightest part on the board, not from a standard. |
| I-024 | low | One dangling via on `+3V3_SENS` at (67.80, 25.92): a pre-placed channel supply via whose stub did not survive the Specctra round trip. It reaches the plane but nothing else, so it is a drill hit that does no work. `drop_dangling_vias` does not catch it - KiCad's connectivity reports it as connected while DRC reports it as one-layer. Warning, not an error. |
| I-006 | medium | Eight silkscreen warnings remain (6 overlap, 2 over pads) plus one starved thermal. |
| I-054 | high | **kicad-tool creates a new symbol by cloning an existing one, and the clone inherits the donor's `MPN`/`Manufacturer`/`LCSC`.** Found 2026-09-16 while adding the LM5164 input stage: all six new capacitors came out claiming `CC0805KRX7R9BB104` - a **100 nF** part - and all six new resistors claiming `0805W8F1002T5E`, a **10 k** part. The buck's input, output, bootstrap and ripple capacitors would all have been ordered as 100 nF, and its on-time, feedback and UVLO resistors all as 10 k. **Nothing warned.** The values were right, the schematic was right, **ERC passed 0/0 and the netlist fingerprint passed IDENTICAL** - because none of those look at MPN. The only thing that would have caught it downstream is the parts arriving. Fixed by `check_mpn_consistency.py`, new, which compares every symbol's MPN against what its *value* resolves to in `passives_catalog.json`; `--fix` rewrites the mismatches. It found 12 and corrected them, and now reports **128 checked, all match**. Not closed: the check is not yet wired into `run_all.ps1`, and it can only verify the 128 symbols whose value is a catalog entry - the other 64 (ICs, connectors, semiconductors) have no machine-checkable source of truth. |
| I-051 | medium | **BOM now carries part numbers; 15 electromechanical parts still unchosen.** Downgraded from high on 2026-09-15. **164 of 192 symbols** now have `MPN`/`Manufacturer`/`LCSC` properties and `kicad-cli sch export bom` emits them - verified by exporting one. `docs/TOOLS.md` now asks for those fields instead of `Description,Datasheet`. Nine test points are deliberately skipped: they are bare pads with nothing to order. **Semiconductors sourced for the first time** (they were never in `passives_catalog.json`, which is a *passives* catalog): `SS34` `C8678` **basic**, `SMBJ33A` `C173526`, `1N4007` `C727081`, `SMAJ6.0CA` `C19077526`, `BAV199` `C5184419`, `2N7000` `C9114`, `PTC` `ASMD1206-050-30V` `C2982267`. **Two more footprint/rating defects caught by cross-checking package against footprint** - the same class as the crystal: `D3` was `Diode_THT:D_DO-41` (through-hole) with no through-hole `1N4007` stocked anywhere at LCSC, corrected to `D_SMA` (16.7 mA of flyback, an SMA 1 A part is ample); and the PTC candidates were **24 V-rated on a 24 V rail**, so `ASMD1206-050-30V` was chosen for headroom. **12 of the 15 assigned 2026-09-15**, each checked against the footprint it must sit in: `D4` `3A4GUD` (`C52034646`, 3 mm through-hole **green** - lit means the contact is closed and the machine may run), `J2` `ZX-PZ2.54-1-16PZZ` (`C7501270`), `J5` `PZ254V-12-6P` (`C492420`), `J6` `PZ254V-12-10P` (`C492422`), `JP1`-`JP3` `PZ254V-11-02P` (`C492401`, 1.29 M in stock; the shunts are `C100114`, ordered separately - they are hardware, not a schematic symbol), `SW1`-`SW5` `TS665CJ` (`C393938`, 6x6 mm 4-pin through-hole, matching `SW_PUSH_6mm`). **All 15 assigned; `J1`/`J3`/`J4` closed with `I-053` on 2026-09-15** (`WJ128V-5.0-3P` `C8270`, `WJ128V-5.0-04P-14-00A` `C192769`). The BOM's only remaining blank MPNs are the nine test points, which is correct - they are plated holes, nothing to order. **Note for regeneration:** `ensure_parts()` skips existing symbols so these properties survive a normal generator run, but a from-scratch rebuild would lose them; the generator does not emit MPN. Original text: **The REV A1 parts must be fully specified before the schematic is called final** - the owner's condition on 2026-09-15. Sweep done that day; three of five gaps closed, two open and both are real. **Closed:** `22k` -> `0805W8F2202T5E` (`C17560`), `4k7` -> `0805W8F4701T5E` (`C17673`), `22p C0G 50V` -> `CL21C220JBANNNC` (`C1804`) - all **JLCPCB basic**, so no setup fee; `source_passives.py` now carries their requirements and reports 26 resolved / 0 unresolved. **Closed 1 - crystal.** `X49SM8MSD2SC` (`C12674`, JLCPCB basic, 158 k stock, $0.0995, +/-20 ppm, -20..+70 C, **CL = 20 pF**); footprint corrected to `Crystal:Crystal_SMD_HC49-SD` and value to `8MHz 20pF`. The original **through-hole** footprint against a surface-mount part would have made the board unbuildable - the single worst defect this sweep found. Original text: **Open 1 - crystal footprint mismatch.** The only 8 MHz basic part at LCSC is `X49SM8MSD2SC` (`C12674`, 158 k stock, $0.0995) and it is **HC-49S SMD**, while `populate_schematic.py` specifies `Crystal:Crystal_HC49-4H_Vertical`, a **through-hole** footprint. One of the two has to change; SMD is the better fit for a JLCPCB-assembled board and keeps the part basic. **Closed 2 - the load capacitors are fine, and my first reading of this was wrong.** 22 pF a side with ~4 pF stray gives CL_actual = 15 pF against the crystal's 20 pF, pulling `C1/2 * [1/(C0+CL_a) - 1/(C0+CL_s)]` ~= **+75 ppm**. A UART tolerates about +/-2% (20 000 ppm) of combined TX+RX error, so that is **267x inside budget**. What actually made `R-9` unachievable was the internal RC at 3-10% (30 000-100 000 ppm), and the crystal fixes that either way. 30 pF would give +12 ppm but is an LCSC *extended* part (20 k stock) against 22 pF *basic* (544 k), and under-loading raises startup margin rather than risking a crystal that will not start - so 22 pF stays, with the arithmetic in the source. Superseded text: **Open 2 - the load capacitors may be the wrong value.** `C60`/`C61` were set to 22 pF from the generic AVR reference circuit, not from the chosen crystal. Load capacitance is `C1*C2/(C1+C2) + C_stray`, so 22 pF a side with ~5 pF stray gives **~16 pF**; if the crystal wants `CL` = 20 pF the oscillator runs fast. **The crystal exists specifically to make the UART accurate enough for Modbus (`R-9`, deferred in `docs/decisions/0010`)**, so getting `CL` wrong defeats the reason for fitting it. Needs the chosen crystal's `CL` from its datasheet, then `C = 2*(CL - C_stray)`. **Also still outside any catalog** (pre-existing, not new): `SS34`, `SMBJ33A`, `1N4007`, `SMAJ6.0CA` x2, `PTC 0.5A`, and `BAV199` x16 - LCSC `C5184419`, $0.0137, 106 k stock, extended not basic. `passives_catalog.json` is a *passives* catalog; the semiconductors need an equivalent or a merged BOM. |
| I-045 | medium | **Input protection is now in the schematic (2026-09-15); the layout items remain.** `D7`-`D22`, two BAV199 per channel on `TC*_FILT_*`, chosen over a 3.3 V TVS on leakage - `docs/decisions/0014` has the numbers. Still open for the rest: stitching vias, the `U12` decoupling distance, and the board-edge pour, all of which are placement and wait on the regeneration. Original finding: **EMC pre-compliance run 2026-09-13** - first time the `emc` skill has been run on this board. 122 findings, risk score 4.0/100, target FCC Class B; the raw output is in the gitignored `docs/reference/analysis/2026-09-13_1128/emc.json` and the volume is misleading, because most rules fire once per net. Triaged against the board rather than reported as a list. **Real, and REV A1 candidates:** (a) **no ESD/surge protection and no common-mode choke on the eight thermocouple inputs** (`IO-001`). The RC filter *is* there and the detector missed it - measured from `pcb.json`, `JTC1` has `R1`/`R2` (100R 0.1 %) at 7.0/8.6 mm and `C1`/`C2`/`C3` at 10.4-12.4 mm - but a resistor and a capacitor are not surge protection, and these are 50 m cables into an engine room. This is the one finding worth acting on. (b) **No ground stitching vias at layer transitions**, 21+ nets (`RP-001`). Freerouting does not place return vias and nothing in the pipeline adds them afterwards; a stitching-via pass belongs in `route.py` or as a post-step. (c) **Ground pour does not reach the board edge** - 160/160 perimeter sample points have no nearby ground zone (`BE-002`). Low confidence: the detector says it uses a bounding-box approximation, and the three pours are per-island by design, so confirm in KiCad before acting. (d) `U12` (IA0505S) nearest decoupling cap is **8.5 mm** away (`DC-001`), against the 2-3 mm the rule wants. Real but modest. **Checked and dismissed:** `IO-001` on `J1` is a threshold artefact - `F1` is at 10.0 mm, `D1` at 17.0 mm and `D2` (SMBJ33A) at exactly **25.0 mm**, the detector's cut-off. Protection exists; that the TVS sits 25 mm behind the entry is worth remembering for `I-028`, not a defect now. `GP-005` *3 ground domains* is the three isolation islands, intentional and on record. The bulk of `GP-001` (54 of 58) is one warning per net for partial plane coverage across those same island boundaries. `CK-001/002/003` concern SPI clocks on outer layers - the bus runs at ~1 MHz, so the harmonics are low-energy. `EE-002`/`SW-001` are the TSR 1-2450 switching at 0.50 MHz, expected of any switcher and relevant to conducted emissions on the 24 V input if `I-028` moves to a battery. **Nothing here is fixed**: REV A0 is frozen (`docs/decisions/0010`) and every item above is layout, so they wait for the same REV A1 as the crystal and the run-permit read-back. **The `spice` skill could not be run** - it needs ngspice, LTspice or Xyce on PATH and none is installed; KiCad's ngspice is a library inside the GUI, not a CLI. |

## Firmware

These are failure modes for a protection device, not polish.

| # | Sev | Issue |
|---|---|---|
| I-013 | high | **Specified now, still not measured.** The budget is in `firmware/include/app/app_config.h` and a `#error` fails the build if the constants drift out of it: 100 ms converter staleness + 456 ms scan + 57 ms loop + 10 ms relay = **623 ms** against the 1 s requirement. Every number is calculated. Measuring it on hardware - step the input past the setpoint, scope the contact - is still open, and the budget is only valid with the clock fuse set (`firmware/fuses.md`). |
| I-016 | medium | **Divider added 2026-09-15; waiting on the board.** `R53` 22k / `R54` 4k7 from `RELAY_LOW` to `PC2` now exist as `RUN_PERMIT_SENSE`, `BOARD_HAS_RUN_PERMIT_SENSE` is `1`, and the firmware builds at 5592 B (17.1%), up 36 B for the newly-live code. `J6` pin 3 was disconnected in the same change so the read-back cannot be forced from the service header. Closes when the board carries the parts. Original text: `RUN_PERMIT` is a static level with no read-back: a stuck pin, a shorted trace or a welded relay permits running forever and the firmware never knows. **Firmware side is written and waiting** - `hal/run_permit.c` implements the check and `BOARD_HAS_RUN_PERMIT_SENSE` in `mcal/board.h` compiles it out, because `PC2` is unconnected on REV A0 and reading it would invent faults from noise. Needs the `R53`/`R54` divider from `RELAY_LOW`, which is in the REV A1 change set (`docs/decisions/0010`). |
| I-032 | high | **Both halves done in the schematic and firmware 2026-09-15; waiting on the board.** `Y1` 8 MHz with `C60`/`C61` 22 pF now drives `U1` 12/13, and the low fuse moved `0x24` -> `0x3F` (`CKSEL=1111`, `SUT=11`) with `program.ps1` defaulting to it. **A chip fused `0x3F` on a board with no crystal looks bricked** - `fuses.md` says so and `-LowFuse 0x24` is the escape. Closes when the regenerated board carries the footprint. Original text: **Fuse specified; the crystal is deferred to REV A1.** `firmware/fuses.md` now pins `CKSEL = 0100` (internal RC 8 MHz) and BOD at 4.0 V, and `program.ps1` verifies both by read-back. What remains is the hardware half. Original text: **No crystal, and the clock fuse is unspecified.** `U1.12/13` (XTAL1/XTAL2) are deliberately unconnected (`populate_schematic.py:474`) and there is no oscillator in the BOM, so the ATmega32A runs on its internal RC. Two consequences nothing in the repo covers: (a) the factory `CKSEL` default is internal **1 MHz**, but the firmware builds at `F_CPU=8000000UL`, so every `_delay_ms` runs 8x long — the 500 ms scan becomes 4 s and `R-3`'s 1 s trip is missed by four times, silently, on a correctly built image; (b) the internal RC is spec'd to a few percent over voltage and temperature, which is outside what an 8-bit UART frame tolerates, so `R-9` (Modbus RTU) is **not** "hardware ready" as `docs/GOAL.md` claims — and the board is already routed with no crystal footprint. Extends `I-015`, which names only `BOD`. No decision record exists for crystal versus internal RC. |

## Process

| # | Sev | Issue |
|---|---|---|
| I-056 | high | **External review of the doc system, 2026-09-17, checked against the repo. Owner has not yet chosen what to act on.** Confirmed: `ISSUES.md` breaks its own one-line rule (49 KB; `I-028` is one 7 215-char row) and is the only core file with no size limit; the Measured table in `STATE.md` is overwritten each session, so there is no dated evidence chain; no index over `decisions/`; no requirement -> evidence traceability (grep for `R-` already false-matches `R-78HB`); `I-028`/`I-002` are part-resolved with open sub-items, so their status cannot be reported; no safety case. Rejected: "not machine-checkable" - the table parses fine and duplicate IDs, undated closures and bad severities are all at zero today; the gap is that nothing runs the check. **Tension to respect:** most proposed fixes are new `docs/` files, which the rule above this table forbids for a recorded reason. Proposed order: split `I-028`/`I-002`; dated evidence log; row-length limit; lint in CI; decisions index; traceability; safety case. **Hardware comes first if the goal is a working board** - still 0 wires. |
| I-046 | medium | **Konnect is AGPL-3.0 and this is a company product.** `docs/decisions/0013` adopts it as the incremental-edit tool. The project states that businesses building on or around it must open-source that work under the same licence, and sells commercial licences. The likely reading is that this does not reach us - a schematic is not a derivative work of the editor that drew it, the same way a binary is not a derivative work of the compiler - but that is a reading, not advice, and Powerhouse is shipping a real unit. **Exposure today is zero**: nothing has been distributed, no Konnect code is in this repository, and the binary lives outside it at `D:	ools\konnect`. What would change that is shipping something built *around* Konnect, or redistributing it. Settle before either. |

## Closed

| # | Closed | What fixed it |
|---|---|---|
| I-061 | 2026-09-26 | **REV A1 board regenerated and routed.** J3 moved to K1 (`0018`); R59/R60 turned to 270 so R60's GND pad is not walled in; the MAX31856 GND pins (1, 14) get fixed vias like the supply pins (`board/stitching.py`) - left to the pour, one per run floated (U3.14, then U5.14); `route.py` re-stitches pour pieces after closing gaps. Measured on the workstation, KiCad 10.0.3: route.py "0 unconnected pad pairs"; `check_board.py` all 6 ok; `kicad-cli pcb drc --refill-zones --schematic-parity` on a copy: **0 errors, 0 unconnected, 0 parity**, 14 silkscreen warnings (8 overlap, 6 over copper, `I-006`). `validate.ps1` stops on ERC `I-063`, not on the board. Freerouting's own "157 violations" is its plane-less model, not DRC. |
| I-060 | 2026-09-26 | Kept the 0.2 mm thermal vias, board minimum 0.2 mm (`0017`); DRC shows no `drill_out_of_range`. Check the fab quote. |
| I-002 | 2026-09-24 | **The schematic is a drawing.** Every block laid out by function and wired: eight identical channels (connector, 100R pair, filter, BAV199 clamps, MAX31856, CS pull-up, MISO series R), the two isolators with their series resistors, the isolated 5 V / LP2985 rail, the MCU with crystal/reset/LCD/buttons/ISP, RS-485, the relay, and the 24 V input with the LM5164. One label per wired group, placed on its wire where the text fits. Done through Konnect by `hardware/8ch/sch_layout/build.py`, which starts from the last label-only schematic and is repeatable. Measured: netlist **IDENTICAL** to `netlist-baseline-reva1.json` (201/162/645), **ERC 0 errors, 0 warnings** (the 171 `endpoint_off_grid` are gone), `check_mpn_consistency.py` 128/128. What is left is cosmetic: `I-059`. Found `I-058` on the way. |
| I-057 | 2026-09-24 | `U12` relinked to `Converter_DCDC_Isolated:IA0505S` with Konnect `replace_component`, and `populate_schematic.py` changed to match. The KiCad 10 `IA0505S` extends `IA0305S`, so the pins are identical - netlist IDENTICAL - and it is the dual +/-5 V part the board uses (+Vout -> `+5V_ISO`, 0V -> `GND_SENS`, -Vout unused). ERC now reports 0 warnings in the cloud as well as on the workstation. |
| I-053 | 2026-09-15 | **The pitch was never the problem - the part number was a two-way block on a three-way footprint.** Owner ruled that 0.08 mm of pitch does not matter and nothing is to be re-footprinted; checking the order code before applying that ruling showed the premise was wrong in a worse way. Phoenix **1935161 is `PT 1,5/ 2-5,0-H`: 2 positions, 5.0 mm pitch** - not an MKDS at all. So the pitch on the footprint (5.000 mm) was right all along, and what would have arrived is a connector with **one terminal too few** on ten positions - eight thermocouple inputs plus `J1` and `J3`. The MKDS 1,5/3-5,08 the row assumed is order code 1715734. **Fixed without touching a single footprint**, exactly as the owner asked: all ten move to `WJ128V-5.0-3P` (`C8270`, KANGNEX, 5.0 mm, 3 positions) and `J4` to `WJ128V-5.0-04P-14-00A` (`C192769`) - one family, 5.0 mm throughout, stocked at LCSC where this board is sourced, instead of a Phoenix specialty import. `populate_schematic.py` was corrected too, so a rebuild cannot put the wrong number back. Verified: ERC **0 violations**, netlist **188/155/613 IDENTICAL**, BOM exports the new codes on all eleven. Residual, accepted: the footprint's silkscreen body is the Phoenix outline, so the KANGNEX body may sit a fraction outside it - pads and pitch are what solder, and this is a cosmetic courtyard note to check at layout, not a fit problem. |
| I-052 | 2026-09-15 | **`populate_schematic.py --refresh-properties` resets symbol POSITIONS, not just properties, and it silently undid the whole `I-002` re-layout.** Run to make `Y1` pick up its corrected footprint, it moved every symbol back to the coordinate in the generator's part table: `U1` from x 326 to x 29, `U15` from 538 to 254, and ERC went **0 -> 188 `endpoint_off_grid`** because the off-grid table coordinates came back with them. The netlist stayed correct, so `netlist_fingerprint.py` did **not** catch this one - ERC did, which is why the gate hardening in `I-049` mattered. Recovered from the owner's commit `ea0e4ee` (`git show HEAD:... > temp`, verified the extents and paper size, then copied over - `git checkout` was refused as destructive, and verifying first was better anyway). **The flag's name promises far less than it does.** Use a targeted `edit_schematic_component` for a single part's value or footprint; `--refresh-properties` is only safe on a schematic whose layout is still the generator's. |
| I-050 | 2026-09-15 | **A uniform bulk move is not uniform on an off-grid sheet, and it shorted four nets.** Konnect's `bulk_move_schematic_components` snaps each symbol to the 1.27 mm grid **independently**, so `C60` moved with `dy=0` and its y still went 163 -> 162.56, and `U1` moved `+284` from x=42 and landed on 326.39 rather than 326. On a sheet that is off-grid by definition - the 192 `endpoint_off_grid` warnings *are* `I-002` - relative geometry therefore shifts by up to ~0.5 mm and pins that were distinct collide. Four did: `GND_RS485` merged into `+5V_RS485` (**a ground onto a 5 V rail**), `SCK_CTRL` into `GND_CTRL`, `SCK_SENS_RAW` into `GND_SENS`, `RS485_RE_N` into `RS485_DE`. `netlist_fingerprint.py` caught all four; nothing else would have. Two were a passive's pin landing on an IC pin (`C41`/`C42` on `U10`), fixed by moving the passive 3 grid units. The other two were subtler and are `I-049`'s mechanism again: the stale `GND_RS485` and `RS485_RE_N` labels survived pruning **because they had come to rest on a different component's pin** (`U15.19` and `U15.6`), so `prune_dangling_labels()` saw them as legitimately placed. **The rule that follows: once a sheet is on-grid, move only in exact multiples of 1.27 mm** - then the snap is a no-op, which was confirmed on the repair move (all four landed at exactly -3.81, no y drift). |
| I-049 | 2026-09-15 | **A stale label survived a net rename, and the gate could not see it.** Adding the REV A1 parts moved `U1` pin 24 from `SPARE_PC2` to `RUN_PERMIT_SENSE`, but `ensure_labels()` only adds labels and `prune_dangling_labels()` only removes labels **not** on a pin - and this one *was* on a pin. Both names ended up on the same node. ERC saw it: *"Both RUN_PERMIT_SENSE and SPARE_PC2 are attached to the same items; RUN_PERMIT_SENSE will be used in the netlist"*. **KiCad picked the winner, not us, on a safety read-back net.** The netlist happened to be right, which is why the fingerprint diff looked clean. `[multiple_net_names]` is a **warning**, and `Assert-ErcReport` threw only on errors - so the gate was green. Found by reading the report, which is not a gate. Fixed twice over: the stale label deleted, and `Assert-ErcReport` now fails on any warning class outside an explicit tolerated list. Only `endpoint_off_grid` is tolerated, with the reason recorded in the code - 192 of them, all of `I-002`, tracked. Tested both ways: the real report passes, an injected `multiple_net_names` is rejected and the message names the offending class. |
| I-048 | 2026-09-15 | **The project's core tool lived in `%TEMP%` and a reboot deleted it.** `kicad-tool` was installed to `%LOCALAPPDATA%\Temp\thermo-kicad-tool-venv`, which Windows is entitled to clear - and did, taking the entire generative pipeline with it: `ModuleNotFoundError: No module named 'kicad_tool'`. Reinstalled with `uv tool install git+https://github.com/mash/kicad-skills.git` to `~/.local/bin`, which persists. **Never `pip install kiutils`** - that pulls upstream, which lacks the API this fork needs. All five references updated to prefer the persistent path and fall back to the old one: `run_all.ps1`, `validate.ps1`, `populate_schematic.py`, `output/render_review.py`, `docs/TOOLS.md`. Also learned the hard way that `kicad-tool` shells out to `kicad-cli` and needs `KICAD_CLI` set - `run_all.ps1` does it, a bare shell does not, and the failure is a bare `[WinError 2] The system cannot find the file specified`. |
| I-047 | 2026-09-14 | **Found while sequencing `I-002`, fixed before it could fire.** `prune_dangling_labels()` in `populate_schematic.py` deletes every label not sitting exactly on a pin coordinate. Its docstring states the premise - *"connectivity in this schematic is carried entirely by labels placed on pin endpoints"* - and **`I-002` is the task that voids it**: a wired sheet legitimately carries labels on wire segments away from any pin. **The path was live, not hypothetical.** `run_all.ps1` calls `populate_schematic.py --labels-only` as step **1/8 of every run**, including a plain validation, and `__main__` prunes unconditionally - the `--labels-only` switch only skips `ensure_parts()`. So the first `run_all.ps1` after the redraw would have deleted the new connectivity from the file. `netlist_fingerprint.py` is step 3 and would have failed loudly afterwards, but by then the schematic on disk is already damaged - the gate reports the corpse, it does not prevent it. Fixed by a guard: if the sheet matches `\(wire\b`, skip pruning and say so, because ERC is the right tool for dangling labels once a drawing exists. Verified both ways - the real schematic today has **0 wires** so pruning is unchanged, a KiCad-written `(wire (pts ...))` triggers the skip, and `(bus (pts`, `(wire_note` and `"wire"` do not false-positive. `docs/STATE.md` had carried this as a one-line warning since the reverted redraw; it is now code. |
| I-041 | 2026-09-08 | Verified by reading inside the archive, not by trusting the rename. `thermocouple_8ch.pdsprj` is a zip; its `ROOT.DSN` now references `thermo_8ch_max6675_sim.hex`, and that file exists in `firmware/build/` alongside the three other images. The obsolete `thermocouple_meter_max6675_8ch.hex` name is gone. **Proteus itself was not launched**, so this closes the stale-filename defect and nothing more - the simulation has still never been run against the current firmware, and the MAX6675 image remains a simulation aid whose converter is not the one on the board. |
| I-035 | 2026-09-08 | Owner chose X7R; `docs/decisions/0011` has the reasoning and what was rejected. `populate_schematic.py` now writes `100n X7R 50V` for `C1`/`C6`/`C11`/`C16`/`C21`/`C26`/`C31`/`C36`, and `passives_catalog.json` resolves it to `CC0805KRX7R9BB104` (JLCPCB basic, 15.7 M in stock). Acceptable in this position specifically: the capacitor sits across `T+`/`T-` with no DC bias so X7R's voltage coefficient does not apply, the board is in a panel 50 m from the engine so nothing excites its piezoelectric response, and the 10:1 ratio against the C0G common-mode pair - the part CMRR actually depends on - is unchanged. Verified after the edit: ERC 0 errors / 171 `endpoint_off_grid` warnings, DRC **0 unconnected and 0 schematic parity issues**, netlist 167 components / 154 nets / 555 pins. The PCB was not touched. |
| I-044 | 2026-09-08 | Found by the same run, and recorded because the root cause is different from `I-035`. `populate_schematic.py` has specified `C48` (the LP2985-3.3 output bypass feeding all eight MAX31856 and both isolators) as `4.7u X7R 16V` since commit `f60e6d3`, while the committed schematic still carried `1u X7R`. Nothing noticed, because `ensure_parts()` only writes properties for symbols that do not yet exist - an existing symbol keeps its old value unless `--refresh-properties` is passed, and nothing in `run_all.ps1` or `validate.ps1` compares the schematic against its own generator. The generator is the source of truth, so the schematic has been corrected to 4.7 uF and `passives_catalog.json` now carries `CGA0805X7R475K350MT` for it. **The gate gap is the real finding**: `validate.ps1` checks the schematic as committed, so any future drift between generator and schematic is invisible again. A regenerate-then-compare step would close it, but that writes the schematic, so it needs the owner's view on where it belongs relative to the REV A0 freeze. |
| I-040 | 2026-09-08 | Verified 2026-09-08 line by line; every item it lists was already reconciled. `GOAL.md` R-8 now reads *EEPROM implementation exists* and R-9 *deferred for REV A0 by decision 0010*. `AGENTS.md` and `hardware/8ch/README.md` state *no VFD in the installation* - the corrected environment from `0009`, not the old premise. The hardware README says the AP2112K *was replaced by* LP2985-3.3, and records that the passive catalog exists while the 100 nF filter specification still conflicts (`I-035`). `STATE.md` is 60 lines against its 60-line limit, down from 86, and no longer lists closed work as pending. |
| I-005 | 2026-09-08 | Closed as **accepted**, not fixed. DRDY and FAULT on all eight MAX31856 stay unconnected because both isolators are fully allocated; the reasoning is on record in `docs/decisions/0004`. Fault detection reads the status register instead, and since `I-038` the driver also re-reads CR0/CR1 every scan, so a converter that stops answering is caught without those pins. Nothing further to do unless a REV A1 frees isolator channels. |
| I-008 | 2026-09-08 | Closed as **not applicable**. The Murata NCS1S is not on the board and was only ever a candidate for a wide-creepage regulated isolated module; `docs/decisions/0002` chose the LP2985-3.3 path instead and that is what is fitted. Its unknown control-pin logic cannot affect this design. Reopen only if that module is revisited. |
| I-036 | 2026-09-07 | Verified 2026-09-07 by running the gate, not by reading the diff. `main_8ch.c` now keeps `candidate_setpoint_x10` and `active_setpoint_x10` apart: UP/DOWN move only the candidate, protection only ever evaluates the active value, and a save that cannot be read back raises `APP_TRIP_CAUSE_SAVE_FAILED` and discards the candidate instead of the stored limit. `tests/test_app_integration.c` drives the real `main_8ch.c` against HAL doubles and passes. |
| I-037 | 2026-09-07 | Verified 2026-09-07. The `config_locked` branch in `app_format_status_line` now comes **after** the editing branch, so a blank unit shows the candidate limit while still refusing to permit running. Covered by `test_app_integration.c`. |
| I-038 | 2026-09-07 | Verified 2026-09-07, and fixed more strongly than the issue asked for. `configuration_ok()` re-reads CR0 and CR1 and checks them against the masks on **every scan**, not once after init, so a MISO stuck at 0x00 or 0xFF fails immediately instead of reading a plausible 0 degC as valid for ~55 s. A recovered channel also burns one settling scan so it cannot report a conversion that was still running. The residual limit is documented in the source: a MISO stuck at a level that happens to satisfy both masks would still pass, because register read-back is not bus verification. `tests/test_bank_driver.c` passes. |
| I-039 | 2026-09-07 | Verified 2026-09-07 by running it. `check_commands.ps1` provides `Invoke-CheckedNative` plus report assertions; `validate.ps1` checks a snapshot copy and never writes the source board; `test_gates.ps1` injects failures and confirms all eight gates reject them. Three defects in the gates themselves were found while exercising them and fixed - see `I-043`. |
| I-042 | 2026-09-07 | Verified 2026-09-07 by running the gate. `sources/check_lists.py` is now invoked by `build.ps1`, the `Makefile` and firmware CI (10 lists, 19 source files, all accounted for). `tests/test_app_integration.c` links the real `main_8ch.c` against `tests/host_mocks/`, and `tests/test_bank_driver.c` drives the real MAX31856 bank driver against a register model. All three suites report `all checks passed` and `build.ps1` exits 0. |
| I-043 | 2026-09-07 | Found while running the gates for the first time end to end, all three fixed. (1) `build.ps1` exited 1 while every suite passed: `VsDevCmd.bat` writes a harmless "vswhere.exe is not recognized" line to stderr and `ErrorActionPreference = 'Stop'` promotes a native tool's stderr to a terminating error. Native calls now go through `Invoke-Native` and are judged on exit code. (2) `test_gates.ps1` and `validate.ps1` returned a non-zero exit on success - `test_gates.ps1` deliberately runs a command that exits 17 as one of its fixtures and `$LASTEXITCODE` kept that value - so CI would have read a passing gate as a failure. Both now `exit 0` explicitly. (3) Worst of the three: `validate.ps1` printed "Sources unchanged" **without having hashed anything**. Windows PowerShell 5.1 will not bind plain strings from the pipeline to `Get-FileHash`, so both the before and after lists were empty and `Compare-Object` compared two empty sets. A gate claiming a check it never performed is worse than one that fails. Hashing is now by `-LiteralPath` and refuses to report success unless every file was hashed; `validation.json` records four real SHA-256 values where it recorded none. |
| I-030 | 2026-09-07 | Wrote `src/hal/temperature_max31856_bank.c`: eight MAX31856 on one bus, per-channel configured/not-configured state, configuration read back after writing, and a channel that loses the bus re-configured on its next scan instead of taking the bank down. The board has firmware for the first time. |
| I-031 | 2026-09-07 | Added `hal/run_permit.{h,c}` with permit semantics in the name, kept `hal/alarm_output.*` for the superseded board, and stopped tracking every `.hex` in git - a stale binary with the opposite polarity was the actual hazard. `program.ps1` warns when the image is not the eight-channel one. |
| I-011 | 2026-09-07 | `app/sensor_monitor.c`: a reading that has not moved 0.1 degC in 120 scans is frozen, and a jump beyond 50 degC in one scan is not physics. Either marks the sample invalid, which latches the trip. Host-tested including the case of a channel returning from a fault. |
| I-012 | 2026-09-07 | `app/settings.c` + `hal/settings_store.c`: setpoint in EEPROM behind a CRC-16, written then read back. An invalid record does **not** fall back to a working setpoint - there is no safe guess for someone else's engine - it config-locks the unit, which shows `SET SETPOINT` and refuses to permit running until an operator stores one. |
| I-014 | 2026-09-07 | `mcal_spi_transfer` now reads `SPDR` on the timeout path, so `SPIF` clears and the byte stream cannot slip permanently. The re-init path is in the new bank driver. |
| I-015 | 2026-09-07 | `firmware/fuses.md` + `firmware/program.ps1`, which reads the fuses back and fails on a mismatch. **The issue text was wrong and is corrected there**: `U1` runs from `+5V_CTRL`, not 3.3 V, so the brown-out level is 4.0 V. It also found that `JTAGEN` is enabled by default and occupies `PC2`-`PC5`, and that `EESAVE` must be programmed or reprogramming wipes the setpoint. |
| I-017 | 2026-09-07 | `PORTB |= (1 << PB6)` in `mcal_spi_master_init`. |
| I-018 | 2026-09-07 | `HAL_MAX6675_SPI_MODE` defaults to mode 0 - the silicon - and the Proteus target overrides it to mode 1 with a `-D`. Nobody ships the simulation's timing by accident. |
| I-019 | 2026-09-07 | Decoding moved to `hal/tc_decode.c`, which has no AVR headers and links into the host tests. Eight cases including -10 and -200 degC, which is what catches a dropped sign extension. |
| I-022 | 2026-09-07 | `firmware/sources/*.txt` is now the single source list; `Makefile` and `build.ps1` both read it. `sources/check_lists.py` fails if a listed file is missing **or if a `.c` under `src/` is in no list** - the second half is what would have caught `I-030`. |
| I-023 | 2026-09-07 | `include/app/version.h`, shown on the LCD at boot and recorded on the commissioning sheet by `program.ps1`. |
| I-007 | 2026-09-07 | `hardware/8ch/source_passives.py` holds the electrical requirement for all 22 distinct passives and resolves each against LCSC; `passives_catalog.json` is the result with MPN, LCSC code, stock, price and the alternates. 22 of 22 resolved - and the search is what found `I-035`. |
| I-021 | 2026-09-07 | `.github/workflows/hardware.yml` exports the netlist from the committed schematic in a KiCad container and compares it with `netlist_fingerprint.py` against the REV A0 contract, plus a check that every passive still has a part number. |
| I-033 | 2026-09-07 | `README.md` rewritten. It described the project as a 100 degC alarm meter and named the superseded single-channel board as the "final physical design". |
| I-034 | 2026-09-07 | ERC re-run against the real schematic: **171 `endpoint_off_grid`, 0 errors** - confirming the number in `docs/STATE.md` was right. (A 714-violation reading during the session came from a broken intermediate file, not the schematic.) |
| I-010 | 2026-09-07 | `git mv semulation simulation` and the project file from `thermocouble [Autosaved]f.pdsprj` to `thermocouple_8ch.pdsprj`, history kept. Proteus was closed this time. |
| I-020 | 2026-09-07 | Split `generate_board.py` (1211 lines) into a `board/` package of ten modules, 18-258 lines each, by responsibility: config, units, geometry, boardio, placement, silkscreen, zones, copper, stitching, connections. `generate_board.py` is now a 65-line entry point and the stable import surface for `route.py`, `check_board.py` and `close_gaps.py`. Verified identical output: same 166 reference designators, same 16 supply vias, same structural checks, zero courtyard overlaps. |
| I-029 | 2026-09-07 | `docs/GOAL.md` corrected by the user's instruction: R-7 no longer says VFD, R-8 states the setpoint must never be hard-coded, and a new "Where it runs" section records the engine, the cylinder-body sensors, the dedicated panel and the 50 m cable so no agent has to guess again. |
| I-001 | 2026-09-07 | Not nine routing problems but three causes: `add_plane_stitching` filtered on `+3V3_SENS` so five of the six plane nets were never stitched; `finish()` poured the outer grounds *after* stitching so their isolated pieces did not exist yet; and `C56`/`C57`/`R33` sat in a 1 mm strip. Fixed by stitching every plane net, pouring before stitching, adding `stitch_pour_islands`, a 0.6 mm via fallback and a 1 mm placement move. `docs/decisions/0007`. DRC: 0 errors, 0 unconnected. Gerbers in `production/8ch/`. |
| I-009 | 2026-09-07 | `hardware/8ch/README.md` rewritten with the LP2985-3.3 and the measured numbers. |
| I-000 | 2026-09-06 | `Q1` had gate and source swapped — the gate sat on GND and the drive signal on the source, so the MOSFET could never turn on, the relay could never energise and `RUN_PERMIT` could never assert. The machine could not have started. Fixed in `populate_schematic.py`. |
